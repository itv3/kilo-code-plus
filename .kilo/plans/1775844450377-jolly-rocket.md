# Plan: Fix upstream merge checkout failure

## Diagnosis

- The merge tool fails at step 5 while running `git checkout <baseBranch>` in `script/upstream/merge.ts`.
- Git refuses the checkout because untracked files under `packages/ui/src/assets/icons/provider/` would be overwritten by the target branch.
- Those icon paths are tracked in this checkout now, but they were likely created as untracked files by a previous failed/partial upstream merge attempt while the repo was on a branch or commit where `packages/ui/` package metadata/gitignore state differed.
- The script already has a `cleanDirectories()` helper for cleaning untracked artifacts after checking out upstream, but there is no pre-checkout cleanup before the first branch switch.

## Immediate Recovery For The Current Checkout

1. Confirm the listed paths are not carrying local work:
   - `git status --short --untracked-files=all -- packages/ui/src/assets/icons/provider`
   - For any listed `??` file that matters, copy it outside the repo before cleaning.
2. Remove only ignored/untracked files in that provider icon directory:
   - `git clean -fd -- packages/ui/src/assets/icons/provider`
3. Re-run the upstream merge command.

## Code Fix

1. Add a safe pre-checkout cleanup to the upstream merge tool before `await git.checkout(config.baseBranch)` in `script/upstream/merge.ts`.
2. Reuse `git.cleanDirectories()` rather than introducing a new shell path, keeping the cleanup scoped to the known generated/conflicting directory:
   - `packages/ui/src/assets/icons/provider`
3. Log the cleanup so users understand why untracked icon files disappeared.
4. Keep the change minimal because `script/upstream/merge.ts` is shared upstream-related automation.

## Validation

1. Run a dry-run/report-only path if available without modifying branches:
   - `bun run script/upstream/merge.ts --version v1.2.26 --dry-run`
2. If dry-run does not exercise step 5, validate with a targeted read-only/code inspection plus rerun the real merge after cleaning the current checkout.
3. Verify the script no longer aborts at step 5 due to untracked provider icons.

## Notes

- This plan intentionally avoids broad `git clean` usage; the cleanup should not delete unrelated untracked work.
- The current plan mode prevented changing files other than this plan, so no recovery cleanup or code edit has been applied yet.
